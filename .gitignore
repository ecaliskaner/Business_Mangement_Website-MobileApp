gemini-code-*
.gemini/
.system_generated/
*.log

# Runtime-generated demo state (session-local scratch, never commit).
# Anchored with a leading slash so it does not also match src/core/state.js.
/data/state.json
/state.js
